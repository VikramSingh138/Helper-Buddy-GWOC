
export const fetchServices = async (filters?: {
  category?: string;
  pincode?: string;
  search?: string;
}) => {
  // API fetching logic
};

export const bookService = async (
  serviceId: string, 
  dateTime: Date, 
  remarks?: string
) => {
  // Booking logic
};